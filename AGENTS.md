与用户用中文交流。

修改前检查工作区并执行 git fetch；存在远端提交时先安全同步，不覆盖未提交的本地改动。完成必要验证后及时提交并推送到远端。

运行 build.ps1 和 tests/check.ps1 验证改动。测试不得更改用户的 Windows 代理、实际 Codex 配置或正在运行的守护程序。安装和卸载会修改本机运行状态，只有用户请求部署时才运行。

不要提交 auth.json、真实 settings.json、config.toml、路由值、日志、个人路径或生成的可执行文件。日常运行必须是独立 EXE，不依赖 Codex 启动脚本。

本机入口使用 HTTPS 和当前用户存储中的自签名证书。自动检查只能在当前用户的“个人”存储中创建临时证书并在结束时删除，不得把任何证书加入根存储；信任或删除根证书的步骤只在用户请求安装或卸载时执行。
